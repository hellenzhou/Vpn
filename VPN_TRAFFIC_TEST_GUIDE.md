# VPN 流量捕获测试指南

## 📋 测试目的

验证 VPN 隧道是否正确捕获了**所有应用流量**，特别是浏览器流量。

## ⚠️ 核心问题

**现象**：
- VPN 客户端日志显示捕获了 TCP 流量（包括 HTTP/HTTPS）
- VPN 服务器未运行（响应=0）
- **但是浏览器仍然能访问网站**

**可能原因**：
1. 浏览器使用 VPN 启动前的旧连接（未被 VPN 捕获）
2. HarmonyOS PC 系统浏览器有特殊权限绕过 VPN
3. 浏览器有多条网络路径（部分走 VPN，部分走物理网络）
4. VPN 路由配置优先级不够高

---

## 🧪 测试步骤

### 步骤 1：完全关闭浏览器

**为什么**：确保没有旧连接绕过 VPN

**操作**：
1. 关闭所有浏览器窗口
2. 打开任务管理器（Ctrl+Shift+Esc）
3. 查找浏览器进程（如 `browser.exe`, `chrome.exe`, `msedge.exe`）
4. 强制结束所有浏览器相关进程

---

### 步骤 2：清除网络缓存

**为什么**：防止 DNS 缓存影响测试

**操作**（PowerShell 管理员权限）：
```powershell
ipconfig /flushdns
```

---

### 步骤 3：启动 VPN 客户端

**操作**：
1. 点击橘色的"启动 VPN"按钮
2. 等待 5 秒，确保 VPN 完全初始化

**验证**：查看日志，确认以下信息：
```
[扩展能力] ✅ vpnConnection.create() 成功!
[扩展能力] ✅✅✅ VPN 启动成功！
[VPN客户端] TUN读取线程已启动
```

---

### 步骤 4：重新打开浏览器（无痕模式）

**为什么**：无痕模式不会使用缓存

**操作**：
1. 打开浏览器的无痕/隐私模式
   - Chrome/Edge: `Ctrl+Shift+N`
   - Firefox: `Ctrl+Shift+P`
2. 访问一个**从未访问过的网站**（例如：`http://httpbin.org/ip`）

---

### 步骤 5：查看日志分析

**关键日志标记**：
```
🔥🔥🔥 [关键] HTTP/HTTPS连接 #1: 192.168.x.x:xxxxx -> x.x.x.x:443 [HTTPS浏览器流量]
🔥 [关键] 此连接已被VPN捕获并发送到服务器127.0.0.1:8888
```

**统计信息**：
```
[VPN客户端] 📊 数据包统计: IPv4 TCP=50 发送=100 响应=0
[VPN客户端] 🌐 浏览器流量统计: HTTP(端口80)=5 HTTPS(端口443)=45
[VPN客户端] ✅✅✅ 已捕获 50 个HTTP/HTTPS连接！
```

---

## 📊 结果判断

### ✅ 情况 1：VPN 工作正常

**日志显示**：
```
✅ 已捕获 XX 个HTTP/HTTPS连接！
响应=0
```

**浏览器行为**：
- **无法访问网站**
- 显示 "无法连接" 或 "超时" 错误

**结论**：VPN 隧道正确捕获了所有流量，因为服务器未运行，所以浏览器无法访问网站。✅ **这是预期行为！**

---

### ⚠️ 情况 2：浏览器有旧连接

**日志显示**：
```
⚠️ 没有检测到TCP数据包（浏览器流量）！
```

**浏览器行为**：
- 能访问网站

**结论**：浏览器使用的是 VPN 启动**之前**建立的连接，这些连接没有被 VPN 捕获。

**解决方法**：重复步骤 1-4，确保浏览器进程已完全退出。

---

### ❌ 情况 3：浏览器绕过 VPN（双路径）

**日志显示**：
```
✅ 已捕获 50 个HTTP/HTTPS连接！
响应=0
```

**浏览器行为**：
- **仍然能访问网站**（这是您当前遇到的问题）

**结论**：
- VPN **确实捕获了部分流量**（日志中可见）
- 但浏览器**同时使用了另一条路径**（未被 VPN 捕获）
- 可能原因：
  1. HarmonyOS PC 系统浏览器有**内核级特殊权限**绕过 VPN
  2. 浏览器使用了**多条并行连接**，部分走 VPN，部分走物理网络
  3. 浏览器检测到 VPN 连接失败后，**自动回退**到物理网络

---

## 🔧 进一步诊断

### 测试 1：使用命令行工具

**目的**：验证非浏览器应用是否被 VPN 捕获

**操作**（PowerShell）：
```powershell
# 测试 1：Ping 外部服务器
ping 8.8.8.8

# 测试 2：使用 curl 访问网站
curl http://httpbin.org/ip
```

**预期行为**：
- 如果 VPN 工作正常，这些命令应该**超时/失败**（因为服务器未运行）
- 如果这些命令成功，说明它们也绕过了 VPN

---

### 测试 2：使用第三方应用

**目的**：测试其他应用是否被 VPN 捕获

**操作**：
1. 启动 VPN
2. 打开微信/QQ/其他网络应用
3. 尝试发送消息或刷新内容

**预期行为**：
- 应该**无法连接**（因为 VPN 服务器未运行）

---

### 测试 3：检查浏览器包名

**目的**：确认浏览器的实际包名

**操作**：
1. 查看 HarmonyOS 系统浏览器的包名
2. 可能的包名：
   - `com.huawei.browser`
   - `com.ohos.browser`
   - `com.microsoft.emmx` (Edge)

**临时测试**：将浏览器包名添加到 `blockedApplications`，看是否有变化：

```typescript
// 测试代码（在 VPNExtentionAbility.ets 第211行）
this.blockedApplications = ['com.huawei.browser', 'com.ohos.browser'];
```

重新编译并测试。如果浏览器仍然能访问网站，说明：
- 包名不正确，或
- 浏览器使用了更底层的网络接口

---

## 📝 当前代码增强

我已经添加了以下诊断功能：

### 1. 详细记录前 20 个 HTTP/HTTPS 连接
```
🔥🔥🔥 [关键] HTTP/HTTPS连接 #1: 192.168.1.100:54321 -> 93.184.216.34:443 [HTTPS浏览器流量]
🔥 [关键] 此连接已被VPN捕获并发送到服务器127.0.0.1:8888
```

### 2. 增强的统计信息
```
[VPN客户端] 🌐 浏览器流量统计: HTTP(端口80)=5 HTTPS(端口443)=45
[VPN客户端] 🔥 关键测试结论：
[VPN客户端] ✅ 如果此时浏览器无法访问网站 = VPN工作正常
[VPN客户端] ❌ 如果此时浏览器仍能访问网站 = 浏览器有双路径（部分流量绕过VPN）
```

### 3. 路由配置验证
```
[扩展能力] 📌 路由配置（2条）：
[扩展能力]    [1] IPv4默认路由: 0.0.0.0/0 → vpn-tun (gateway: 192.168.100.1)
[扩展能力]        isDefaultRoute: true
```

---

## 🎯 最终结论

根据您提供的日志：
- ✅ VPN 隧道**已正确创建**（tunFd: 55）
- ✅ 路由配置**看起来正确**（0.0.0.0/0 → vpn-tun）
- ✅ VPN 客户端**确实捕获了 TCP 流量**（245 个 TCP 包）
- ❌ 但浏览器**仍然能访问网站**

**最可能的原因**：
1. **HarmonyOS PC 系统浏览器有特殊权限**，可以绕过第三方 VPN 应用
2. **浏览器有双路径机制**，检测到 VPN 失败后自动使用物理网络

**推荐的解决方案**：
1. 使用命令行工具（ping/curl）验证 VPN 是否捕获非浏览器流量
2. 如果命令行工具也被捕获且失败，说明 VPN 工作正常，只是系统浏览器特殊
3. 如果命令行工具也能正常访问，说明 VPN 路由配置需要调整

---

## 📞 下一步

请按照上述步骤重新测试，并提供：
1. **完整的日志**（包含 `🔥🔥🔥 [关键] HTTP/HTTPS连接` 的行）
2. **浏览器的实际行为**（能/不能访问网站）
3. **命令行工具测试结果**（ping/curl 是否成功）

这样我们就能确定问题的根本原因。
